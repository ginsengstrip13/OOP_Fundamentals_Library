<UserControl x:Class="lab11.Views.ContactsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="420"
             d:DesignWidth="760">
    <!--
        Это бывшее главное окно телефонной книги, преобразованное в UserControl.
        DataContext назначается автоматически через DataTemplate в App.xaml.
    -->
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center"
                       Text="Имя:" />
            <TextBox Grid.Column="1"
                     Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <Grid Grid.Row="1"
              Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center"
                       Text="Телефон:" />
            <TextBox Grid.Column="1"
                     Text="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">
            <!-- Все действия выполняются командами ViewModel, а не обработчиками событий в code-behind. -->
            <Button Content="Добавить"
                    Width="110"
                    Margin="0,0,8,0"
                    Command="{Binding AddCommand}" />
            <Button Content="Редактировать"
                    Width="120"
                    Margin="0,0,8,0"
                    Command="{Binding EditContactCommand}"
                    CommandParameter="{Binding SelectedContact}" />
            <Button Content="Удалить"
                    Width="110"
                    Command="{Binding DeleteCommand}"
                    CommandParameter="{Binding SelectedContact}" />
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Margin="0,0,0,8"
                   Foreground="DarkSlateGray"
                   Text="{Binding ValidationMessage}"
                   TextWrapping="Wrap" />

        <DataGrid Grid.Row="4"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding Contacts}"
                  SelectedItem="{Binding SelectedContact, Mode=TwoWay}">
            <!-- DataGrid работает с ObservableCollection<Contact>, поэтому строки обновляются автоматически. -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя"
                                    Width="*"
                                    Binding="{Binding Name}" />
                <DataGridTextColumn Header="Телефон"
                                    Width="*"
                                    Binding="{Binding Phone}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
