<Window x:Class="lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Паттерны: Фабричный метод" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="15" VerticalAlignment="Center">
            <TextBlock Text="Выберите цвет: " VerticalAlignment="Center" Margin="0,0,10,0" FontSize="16"/>
            <ComboBox x:Name="ColorSelector" Width="150" FontSize="14" SelectionChanged="ColorSelector_SelectionChanged">
                <ComboBoxItem Content="Красный" Tag="Red"/>
                <ComboBoxItem Content="Синий" Tag="Blue"/>
                <ComboBoxItem Content="Зелёный" Tag="Green"/>
            </ComboBox>
        </StackPanel>

        <Canvas x:Name="DrawingCanvas" Grid.Row="1" Background="#F0F0F0"/>
    </Grid>
</Window>